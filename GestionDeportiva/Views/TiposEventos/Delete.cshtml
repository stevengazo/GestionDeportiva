@model GestionDeportiva.Models.TiposEventos

@{
    ViewBag.Title = "Borrar Categoría";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h3>¿Esta seguro que desea eliminar esta categoría?</h3>

<div>
    <h4>TiposEventos</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Nombre)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Nombre)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Borrar" class="btn btn-outline-danger" /> |
            @Html.ActionLink("Volver a la lista", "Index",null,new { @class="btn btn-outline-info"})
        </div>
    }
</div>
